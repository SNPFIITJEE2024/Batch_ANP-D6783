angle1 = float(input("enter first angle"))
#validate angle1
if(angle1 <= 0):
    exit("angle must be positive")
    
angle1 = float(input("enter second angle"))
#validate angle2
if(angle2 <= 0):
    exit("angle must be positive")
    
    angle3 = float(input("enter third angle"))
#validate angle2
if(angle3 <= 0):
    exit("angle must be positive")
#verifying a]triangle formation
if(angle1+angle2+angle3==180):
    print("triangle is valid") #triangle is formed
    #acute angled triangle
    if(angle1 < 90 and angle2 < 90 and angle < 90):
        print("triangle formed is acute angled triangle")
    #right angled triangle
    elif(angle1 == 90 or angle2 == 90 or angle3 == 90):
        print("triangle formed is right angled triangle")
    #obstuse angled triangle
    else:
        print("triangle formed is obstuse angled triangle")
else:
    print("triangle formed is invalid")#triangle is not formed
    